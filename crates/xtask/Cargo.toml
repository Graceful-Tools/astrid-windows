[package]
name = "xtask"
description = "Repo automation: contract fixtures, binding generation, packaging checks."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
